# 🔌 MCP Сервер для складской системы

**MCP (Model Context Protocol)** — протокол от Anthropic для интеграции AI ассистентов с внешними системами.

## 🎯 Что это дает?

AI ассистент (Claude, GPT и др.) может:
- 📦 Просматривать товары на складе
- 🔍 Искать товары по описанию
- 📊 Получать статистику
- ➕ Импортировать товары с сайта
- ✏️ Обновлять складские данные
- 🖨️ Создавать карточки для печати

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонируй проект
git clone https://github.com/swiftskytex-coder/warehouse-management.git
cd warehouse-management

# Установи зависимости
pip install -r requirements.txt
```

### 2. Настройка Claude Desktop

Открой файл конфигурации Claude:
```bash
# macOS
~/Library/Application Support/Claude/claude_desktop_config.json

# Windows
%APPDATA%/Claude/claude_desktop_config.json
```

Добавь MCP сервер:
```json
{
  "mcpServers": {
    "warehouse": {
      "command": "python",
      "args": ["/путь/к/warehouse-management/mcp_server.py"],
      "env": {
        "PYTHONPATH": "/путь/к/warehouse-management"
      }
    }
  }
}
```

Или используй готовый конфиг:
```bash
cp mcp_config.json ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

### 3. Перезапусти Claude Desktop

AI теперь видит твой склад!

## 🎤 Примеры использования

Спроси Claude:

```
Покажи все товары на складе
```

```
Сколько товаров от производителя Otis?
```

```
Импортируй товар с артикулом 2498
```

```
Найди товары с низким остатком
```

```
Обнови количество товара 768, поставь 50 штук в зоне A
```

```
Создай карточку для товара 2498
```

## 🛠️ Доступные инструменты

### 📦 Управление товарами

#### `list_products`
Получить список всех товаров
```json
{
  "limit": 50,
  "offset": 0
}
```

#### `get_product`
Получить детали товара по артикулу
```json
{
  "article": "2498"
}
```

#### `search_products`
Поиск по названию/производителю
```json
{
  "query": "красная кнопка"
}
```

### 📊 Аналитика

#### `get_stock_stats`
Общая статистика склада
- Всего товаров
- Заканчивающиеся
- Нет в наличии

#### `get_low_stock`
Товары с низким остатком

### ➕ Импорт

#### `import_product`
Импорт с snab-lift.ru
```json
{
  "query": "2498"
}
```

### ✏️ Обновление

#### `update_stock`
Обновить складские данные
```json
{
  "article": "2498",
  "zone": "A",
  "rack": "12",
  "shelf": "3",
  "cell": "45",
  "quantity_actual": 50,
  "quantity_min": 10,
  "notes": "Важный товар"
}
```

### 🖨️ Документы

#### `create_product_card`
Создать HTML карточку
```json
{
  "article": "2498"
}
```

## 📝 JSON Schema

Все инструменты имеют JSON Schema для валидации:

```json
{
  "name": "update_stock",
  "description": "Обновить складские данные товара",
  "inputSchema": {
    "type": "object",
    "properties": {
      "article": {"type": "string"},
      "zone": {"type": "string"},
      "quantity_actual": {"type": "integer"}
    },
    "required": ["article"]
  }
}
```

## 🔧 Ручной запуск

```bash
# Запустить MCP сервер
python mcp_server.py

# Он будет читать stdin и писать в stdout
# Это stdio transport для Claude Desktop
```

## 🐛 Отладка

```bash
# Проверь, что сервер работает
echo '{"jsonrpc": "2.0", "id": 1, "method": "tools/list"}' | python mcp_server.py

# Должен вернуть список инструментов
```

## 🌟 Преимущества MCP

✅ **Стандартизация** — единый протокол для всех AI  
✅ **Безопасность** — AI не имеет прямого доступа к базе  
✅ **Валидация** — JSON Schema проверяет все входные данные  
✅ **Прозрачность** — видно какие инструменты доступны  
✅ **Расширяемость** — легко добавлять новые функции  

## 📚 Поддерживаемые клиенты

- ✅ **Claude Desktop** — полная поддержка
- ✅ **Claude Code** — через MCP
- 🔄 **GPT** — через адаптеры
- 🔄 **Другие AI** — через MCP SDK

## 🔗 Полезные ссылки

- [MCP Documentation](https://modelcontextprotocol.io)
- [MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk)
- [Claude Desktop](https://claude.ai/download)

---

**Теперь AI может управлять твоим складом!** 🤖📦
