# Интеграция с Airtable для складских карточек

## Преимущества:
✅ Доступ из любого места (веб, мобильное приложение)
✅ Готовый интерфейс (не нужно программировать UI)
✅ Совместная работа (несколько сотрудников)
✅ Автоматизации (триггеры, уведомления)
✅ API для интеграции с парсером
✅ Фильтры, сортировка, поиск
✅ Вложения (фото товаров)
✅ Бесплатно до 1200 записей

## Структура базы:
```
Таблица "Складские карточки":
- Артикул (текст, уникальный)
- Название товара (текст)
- Производитель (связь с таблицей "Производители")
- Категория (выбор из списка)
- Цена (число)
- Фото (вложения)
- Зона склада (выбор: A, B, C...)
- Стеллаж (текст)
- Полка (текст)
- Ячейка (текст)
- Количество факт (число)
- Количество резерв (число)
- Доступно (формула: факт - резерв)
- Статус (выбор: В наличии, Заканчивается, Нет)
- URL товара (ссылка)
- Дата обновления (дата)
- Ответственный (сотрудник)
```

## Как подключить парсер:

### 1. Создай базу в Airtable
- Зарегистрируйся на airtable.com
- Создай новую базу "Склад"
- Настрой таблицы по структуре выше

### 2. Получи API ключ
- Profile → Account → API → Generate API key

### 3. Установи библиотеку
```bash
pip install pyairtable
```

### 4. Код интеграции:
```python
from pyairtable import Table
import os

# Настройки
AIRTABLE_API_KEY = 'your_api_key_here'
AIRTABLE_BASE_ID = 'your_base_id_here'  # Из URL базы
AIRTABLE_TABLE_NAME = 'Складские карточки'

# Подключение
table = Table(AIRTABLE_API_KEY, AIRTABLE_BASE_ID, AIRTABLE_TABLE_NAME)

# Добавление товара
def add_to_airtable(product_data):
    record = {
        'Артикул': product_data['article'],
        'Название товара': product_data['title'],
        'Производитель': product_data['manufacturer'],
        'Цена': product_data['price'],
        'Зона склада': '',  # Заполнит складской работник
        'Стеллаж': '',
        'Полка': '',
        'Ячейка': '',
        'Количество факт': 0,
        'Количество резерв': 0,
        'URL товара': product_data['url'],
        'Фото': [{'url': img} for img in product_data['images'][:5]]
    }
    
    return table.create(record)

# Проверка существования
def check_exists(article):
    formula = f"{{Артикул}} = '{article}'"
    records = table.all(formula=formula)
    return len(records) > 0

# Обновление количества
def update_quantity(article, actual, reserved):
    formula = f"{{Артикул}} = '{article}'"
    records = table.all(formula=formula)
    if records:
        record_id = records[0]['id']
        table.update(record_id, {
            'Количество факт': actual,
            'Количество резерв': reserved
        })
```

## Пример использования:
```python
from warehouse_card import parse_product_page, create_driver

driver = create_driver()
product = parse_product_page(driver, 'https://snab-lift.ru/catalog/.../2498.html')

# Сохраняем в Airtable
if not check_exists(product['article']):
    result = add_to_airtable(product)
    print(f"✅ Добавлено: {result['fields']['Название товара']}")
else:
    print("⚠️ Товар уже существует")
```

## Интерфейс Airtable:
- 📱 Мобильное приложение для складских работников
- 🖥️ Веб-версия для менеджеров
- 📊 Галерея (карточки с фото)
- 📅 Календарь поставок
- 📈 Dashboard с аналитикой

## Стоимость:
- Free: 1200 записей, 5GB вложений
- Plus: $10/мес, 5000 записей
- Pro: $20/мес, 50000 записей

## Минусы:
❌ Зависимость от интернета
❌ Ограничения бесплатного тарифа
❌ Данные на чужих серверах
❌ Месячная оплата при росте
